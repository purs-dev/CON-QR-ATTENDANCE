rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Fully open for this public event. Change to dated rules after if needed.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}